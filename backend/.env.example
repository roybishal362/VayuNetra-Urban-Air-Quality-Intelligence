# VayuNetra backend configuration.
# Every key below is OPTIONAL. With nothing set the platform runs on offline snapshots +
# deterministic templates and never crashes. Add keys to "go fully live".

# ---- LLM (primary: Groq — OpenAI-compatible, fast + low-cost) ----
# Free key: https://console.groq.com/keys
GROQ_API_KEY=
# Optional: several keys from different accounts to multiply the free-tier rate limit.
# The client rotates across them and fails over on 429. Comma-separated; overrides/augments
# GROQ_API_KEY. e.g. GROQ_API_KEYS=gsk_aaa,gsk_bbb,gsk_ccc
GROQ_API_KEYS=
# Strong default. Alternatives: openai/gpt-oss-120b, qwen/qwen3-32b, moonshotai/kimi-k2-instruct
GROQ_MODEL=llama-3.3-70b-versatile

# ---- Optional Anthropic fallback (used only if GROQ_API_KEY is empty) ----
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

# ---- Real active fires (else a seasonal model is used, flagged honestly) ----
# Free key: https://firms.modaps.eosdis.nasa.gov/api/area/
FIRMS_MAP_KEY=

# ---- Real ground stations overlay (else CAMS only) ----
# Free key: https://openaq.org
OPENAQ_API_KEY=

# ---- Citizen alert delivery ----
# Telegram (free, live): @BotFather → /newbot → paste the token. Powers /aqi, /subscribe.
TELEGRAM_BOT_TOKEN=
# SMS + WhatsApp via Twilio (optional). Live the moment these are set. NOTE: India SMS also
# needs a DLT-registered sender ID (TRAI rule) — set twilio_from_sms to that sender.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_SMS=
TWILIO_FROM_WHATSAPP=

ENVIRONMENT=dev
